spring:
  application:
    name: order-service
  kafka:
    consumer:
      auto-offset-reset: earliest
  profiles:
    active: dev
server:
  port: 8050
zeebe:
  client:
    security:
      plaintext: true